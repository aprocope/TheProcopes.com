name: Deploy Theme to Lightsail

# Runs on every push to the main branch
on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy to Amazon Lightsail
    runs-on: ubuntu-latest

    steps:
      # 1. Check out the theme code
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Sync theme files to your Lightsail server via rsync over SSH
      #    This only uploads changed files, making deploys fast.
      - name: Deploy via rsync
        uses: burnett01/rsync-deployments@7.0.1
        with:
          switches: -avz --delete --exclude='.git' --exclude='.github'
          path: ./
          remote_path: ${{ secrets.REMOTE_PATH }}
          remote_host: ${{ secrets.REMOTE_HOST }}
          remote_user: ${{ secrets.REMOTE_USER }}
          remote_key: ${{ secrets.SSH_PRIVATE_KEY }}

      # 3. (Optional) Clear any WordPress cache after deploy
      # - name: Flush cache
      #   uses: appleboy/ssh-action@v1
      #   with:
      #     host: ${{ secrets.REMOTE_HOST }}
      #     username: ${{ secrets.REMOTE_USER }}
      #     key: ${{ secrets.SSH_PRIVATE_KEY }}
      #     script: |
      #       cd /path/to/wordpress
      #       wp cache flush --allow-root
